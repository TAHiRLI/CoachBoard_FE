version: '3.4'

networks:
  app_network:

services:
  coachboard_fe:
    image: coachboard_fe
    build:
      context: app
      dockerfile: ./Dockerfile
      
    environment:
      - NODE_ENV=production
    networks:
      - app_network
    ports:
      - "8080:92"